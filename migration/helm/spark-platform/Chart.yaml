apiVersion: v2
name: spark-platform
description: A Helm chart for the Spark-Delta Lake platform on Kubernetes
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: airflow
    version: 1.10.0 # Using a known stable version, or 1.11.0 etc. 
    repository: https://airflow.apache.org
    condition: airflow.enabled
