apiVersion: v1
kind: Service
metadata:
  name: spark-ui-headless
  namespace: default
  labels:
    app: spark-ui-headless
spec:
  clusterIP: None
  selector:
    spark-role: driver
  ports:
  - name: spark-ui
    port: 4040
    targetPort: 4040
